Explanation

If an attacker steals the database, they shouldn't see plain passwords.
If they intercept the network, they shouldn't see the credit card number.
Failure to do this is Sensitive Data Exposure.
